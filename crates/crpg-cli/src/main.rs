#![forbid(unsafe_code)]
#![warn(missing_docs)]
//! `crpgc` — the campaign toolchain CLI: validate, migrate, pack, run,
//! replay and diff. No Godot, no rendering.

fn main() {
    println!("Hello, world!");
}
