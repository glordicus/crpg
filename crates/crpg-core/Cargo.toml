[package]
name = "crpg-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
indexmap = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
# ADR-0006 dev-only deps: proptest for property tests, serde_json for the
# Serialize/Deserialize round-trip tests. Neither may become a normal
# dependency of this crate.
proptest.workspace = true
serde_json.workspace = true
