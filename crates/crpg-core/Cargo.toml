[package]
name = "crpg-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]

[dev-dependencies]
# ADR-0006 dev-only deps: proptest for property tests, serde_json for the
# Serialize/Deserialize round-trip tests. Wired here so T006a's tests can use
# them; the property tests themselves land with T006a.
proptest.workspace = true
serde_json.workspace = true
